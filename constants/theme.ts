const Background = "#0D1117"; // Dark background for the app
const CardSurface = "#161B22"; // Slightly lighter than the background for cards and surfaces
const AccentPrimary = "#00D4FF"; // Bright cyan for primary accents and highlights
const IncomeGreen = "#00C853"; // Vibrant green for income-related elements
const ExpenseRed = "#FF3B30"; // Bright red for expense-related elements
const TextPrimary = "#FFFFFF"; // White for primary text
const TextSecondary = "#8B949E"; // Light gray for secondary text and less important information

const FontSizes = {
  small: 12,
  medium: 16,
  large: 20,
};

const BorderRadii = {
  small: 4,
  medium: 8,
  large: 16,
};

const SpacingScale = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};

export const theme = {
  colors: {
    Background,
    CardSurface,
    AccentPrimary,
    IncomeGreen,
    ExpenseRed,
    TextPrimary,
    TextSecondary,
  },
  FontSizes,
  BorderRadii,
  SpacingScale,
};
